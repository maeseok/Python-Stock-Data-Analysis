.ndim - 배열의 차원
.shape - 배열 크기
.dtype - 원소 자료형
.max .mean .min .sum
.flatten() - 1차원 배열 형태로 바꿈
.transpose() - 요소 위치를 주대각선을 기준으로 뒤바꿈
.dot() - 두 배열의 내적 곱
.random.rand() - 0~1값의 균일 분포라고 불리는 값을 입력받는 개수만큼 배열로 반환(다차원 배열포함)
.random.randn() - -1~1값의 정규 분폰라고 불리는 값을 위와 같이 리턴
.random.randint(low,high,size,dtype) - 최솟값(혹은 최댓값), 최댓값(혹은 옵션), 얼마나 리턴할지, int or int64
.random.random() - 0~1보다 작은 값 입력받은 개수만큼 배열 반환
.T() - 전치 행렬로 변환시킴(행과 열을 바꿈)
연산은 원소끼리 연산
브로드캐스팅 - 크기가 달라도 연산할 수 있게 크기가 작은 행렬을 확장